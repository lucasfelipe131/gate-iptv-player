{
  "name": "gate-iptv-player",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "GATE IPTV PLAYER — player de mídia para Smart TVs e fontes autorizadas.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "hls.js": "^1.6.17",
    "mpegts.js": "^1.8.1"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
