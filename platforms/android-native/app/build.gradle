plugins { id 'com.android.application' }

android {
    namespace 'com.gateone.app.gateiptvplayer'
    compileSdk 36

    defaultConfig {
        applicationId 'com.gateone.app.gateiptvplayer'
        minSdk 23
        targetSdk 35
        versionCode 62
        versionName '0.6.2'
    }

    // Real Android/Google TV devices use ARM. Keeping x86/x86_64 native VLC
    // binaries in the TCL package more than doubles the APK and can make older
    // TV package installers reject or truncate it. Generate a compact ARM
    // universal APK plus architecture-specific fallbacks.
    splits {
        abi {
            enable true
            reset()
            include 'armeabi-v7a', 'arm64-v8a'
            universalApk true
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    lint {
        abortOnError true
        checkDependencies true
        checkReleaseBuilds true
    }
}

dependencies {
    implementation 'org.videolan.android:libvlc-all:3.7.5'
    implementation 'androidx.media3:media3-exoplayer:1.11.0'
    implementation 'androidx.media3:media3-exoplayer-hls:1.11.0'
    implementation 'androidx.media3:media3-ui:1.11.0'
}
